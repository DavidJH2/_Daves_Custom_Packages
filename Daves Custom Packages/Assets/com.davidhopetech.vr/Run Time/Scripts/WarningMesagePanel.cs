using UnityEngine;

namespace com.davidhopetech.vr.Run_Time.Scripts
{
    public class WarningMesagePanel : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
